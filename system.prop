# ASB V63.416 managed-property build.
#
# This file is intentionally free of property assignments. Magisk loads module
# system.prop before runtime feature checks; optional vendor properties are
# instead stored in runtime/asb_managed.props and applied only by
# runtime/asb_apply_managed_props.sh after a validated device pack confirms
# the exact fingerprint and domain=properties.
